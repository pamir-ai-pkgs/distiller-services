#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@

override_dh_installsystemd:
	dh_installsystemd --name=distiller-wifi

override_dh_fixperms:
	dh_fixperms
	# Make main script executable
	chmod +x debian/distiller-cm5-services/opt/distiller-cm5-services/distiller_wifi.py
	# Make all shell scripts executable
	-find debian/distiller-cm5-services/opt/distiller-cm5-services/scripts -name "*.sh" -exec chmod +x {} \;
